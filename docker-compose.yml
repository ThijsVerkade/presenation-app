services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: presentation-app
    ports:
      - "80:8080"
      - "6001:6001"
    volumes:
        - ./public/media:/var/www/html/public/media
        - ./database:/var/www/html/database
        - /var/run/presentation-control:/tmp/presentation-control
    restart: unless-stopped
    dns:
        - 127.0.0.1
