export interface MuxAssetResponse {
    asset_id: string;
    cors_origin: string;
    id: string;
    status: string;
}

export interface MuxVideoResponse {
    asset_id: string;
    playback_id: string;
    playback_url: string;
    status: string;
}
